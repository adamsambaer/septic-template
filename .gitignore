# Dependencies
node_modules
.pnpm-store

# Next.js
.next
out

# OpenNext / Cloudflare
.open-next
.wrangler
test
# Build
dist
build

# Environment and local Worker secrets. The two *.example files are the
# Deploy Button's prompts and stay tracked.
.env*
!.env.example
!.env.local.example
.dev.vars*
!.dev.vars.example

# NPM credentials
.npmrc

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE
.idea
.vscode
*.swp
*.swo
.DS_Store

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Misc
*.log
screenshots/

# Agency-only outputs. The client snapshot (src/config/site-config.generated.json)
# and pulled photos (public/img/cms/) ARE tracked: a clone must build with no secret.
onboarding/out/
onboarding/template-bundle.json
